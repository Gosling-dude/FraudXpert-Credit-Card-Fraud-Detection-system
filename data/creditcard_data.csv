import pandas as pd
import numpy as np

# Function to generate synthetic credit card data
def generate_creditcard_data(num_rows=350):
    np.random.seed(42)
    
    data = {
        'TransactionID': np.arange(1, num_rows + 1),
        'TransactionAmount': np.random.normal(100, 50, num_rows).round(2),
        'TransactionTime': np.random.uniform(0, 86400, num_rows).round(0),  # seconds in a day
        'CustomerID': np.random.randint(1000, 5000, num_rows),
        'Fraud': np.random.choice([0, 1], num_rows, p=[0.97, 0.03])  # 3% fraud
    }
    
    df = pd.DataFrame(data)
    df.to_csv('./data/creditcard_data.csv', index=False)

# Generate the data
generate_creditcard_data(350)
